<footer>
    <div>
        <button onclick="toggleTheme()">Toggle Theme</button>
    </div>

    <div>
        <!-- About -->
        <h5>About</h5>
        <p>
            Find and rate books through BookSite. Search Google Books to discover more titles.
        </p>

        <!-- Quick Links -->
        <h5>Quick Links</h5>
        <ul>
            <li><a href="{{ path('book_list') }}">Home</a></li>
            {% if app.user %}
                <li><a href="{{ path('book_new') }}">Add Books</a></li>
                <li><a href="{{ path('user_reviews') }}">My Reviews</a></li>
            {% else %}
                <li><a href="{{ path('app_login') }}">Add Books</a></li>
                <li><a href="{{ path('app_login') }}">My Reviews</a></li>
            {% endif %}
            <li>Email: meddycooper@outlook.com</li>
        </ul>

        <!-- Social Links -->
        <h5>Follow Us</h5>
        <a href="https://www.facebook.com/people/Meddy-Cooper/pfbid026Q2XzcApapLSbL25UGKttYddYnqi7TJ96Wf6vooTtK4WvZm8t8cky4AXLnjEcCDzl/">Facebook</a><br>
        <a href="https://www.instagram.com/meddy_cooper/">Instagram</a>
    </div>

    <div>
        © {{ "now"|date("Y") }} BookSite. All rights reserved.
    </div>
</footer>

<script>
    function toggleTheme() {
        document.body.classList.toggle("light-theme");
        localStorage.setItem("theme", document.body.classList.contains("light-theme") ? "light" : "dark");
    }

    window.onload = function() {
        const theme = localStorage.getItem("theme");
        if (theme === "light") {
            document.body.classList.add("light-theme");
        }
    };
</script>
